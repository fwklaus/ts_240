// 1
// the code will run without any issues and it won't throw any errors
// using a type assertion to set the type as  `any` for userSettings turns off the type checking system, effectively

// output:
// light
// ["email", "push", "sms"]