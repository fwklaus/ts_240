// 1

// write function combine
// takes two parameters - either string or number
// concatentaes strings or adds numbesr based on the types of the input parameters
  // use type guard
// return string or number


// type ConcatenateType = string | number;

// function combine(a: ConcatenateType, b: ConcatenateType): ConcatenateType | undefined {
//   if (typeof a === 'string' && typeof b === 'string') {
//     return a + b;
//   } else if (typeof a === 'number' && typeof b === 'number') {
//     return a + b;
//   } else {
//     return undefined;
//   }
// }


// console.log(combine("5", "5")); // "55"
// console.log(combine(5, 5));   // 10
// console.log(combine(5, "5")); // undefined
