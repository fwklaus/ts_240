// 1
// function concatenateStrings(a: string, b: string): string {
//   return a + b;
// }

// function addNumbers(a: number, b: number): number {
//   return a + b;
// }

// const result = concatenateStrings("Hello", "World");
// const numericResult = addNumbers(1, 2);

// type StringOrNum = string | number;

// function combine(a: StringOrNum, b: StringOrNum ): StringOrNum {
//   if ( typeof a === 'string' && typeof b === 'string' ) {
//     return a + b;
//   } else if ( typeof a === 'number' && typeof b === 'number' ) {
//     return a + b;
//   } else {
//     throw new Error("Invalid input types: both inputs must be strings or both inputs must be numbers");
//   }
// }

// const concatenated = combine("Hello, ", "World!"); // "Hello, World!"
// const added = combine(5, 10); // 15
